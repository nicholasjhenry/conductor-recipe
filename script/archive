#!/bin/sh

# script/archive: Cleans up the project when archived

set -e

cd "$(dirname "$0")/.."

eval "$(mise env)"

echo "==> Tearing down docker containers…"
docker compose down --volumes --remove-orphans
